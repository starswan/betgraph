<% @priced_matches.group_by { |m| m.kickofftime.to_date }.each do |date, matches| %>
  <%= link_to_unless date == @date, "#{date.to_fs(:season)} [#{matches.size}]", division_season_fixture_path(@division, @season, date) %>
<% end %>