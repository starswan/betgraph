<%= render 'menu', seasons: @seasons, season: @season, divisions: @season.calendar.divisions, date: @date %>
<div align="center">
  <%= render 'half_time_title', match: @fixture %>
</div>
<% @fixtures.each do |f| %>
  <%= link_to_unless @fixture == f, f.name, half_time_sport_season_fixture_bet_markets_path(f.division.sport, f.season, f) %>
<% end %>
<%= render 'chart', bet_markets: @bet_markets %>
<%= render 'lambda_chart', bet_markets: @bet_markets, name: 'HalfTimeScore', limit: 2 %>
